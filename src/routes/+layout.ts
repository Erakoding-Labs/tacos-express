// Fully prerender the marketing site for fast, CDN-friendly static output
// (works on Vercel and Cloudflare Pages via adapter-auto).
export const prerender = true;
export const trailingSlash = 'never';
